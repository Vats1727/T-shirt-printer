## Packages
framer-motion | Smooth animations for UI elements and transitions

## Notes
Canvas interactions handled via native HTML5 Canvas API
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
