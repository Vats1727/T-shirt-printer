Hey 👋  
I want you to help me build a small web app similar to the Teespring Design Launcher, but only a light and simple version.

The idea is:
Users should be able to type some text, choose a product (like T-shirt, Mug, Hoodie), pick a color, and see a preview image. Then they should be able to save that design.

Please build this using:
- Node.js with Express for backend
- Plain HTML, CSS, and JavaScript for frontend
- HTML Canvas for the image preview
- No external APIs, no database — just save data into a local JSON file.

I want the project to have this structure:
- server.js
- package.json
- public/index.html
- public/style.css
- public/app.js
- designs.json (create it automatically if not present)

Here’s what the app should do:

Frontend:
- A clean, simple page with a centered card
- Title: “Design Launcher”
- Input box for slogan text
- Dropdown for product (T-shirt, Mug, Hoodie)
- Color picker for text color
- Button: “Generate Preview”
- A canvas (around 300x300) that shows the product preview
- Button: “Save Design”
- A small message area to show “Saved successfully” or errors

Preview behavior:
- When I click “Generate Preview”, clear the canvas
- Draw a light gray rectangle as the product base
- Write the product name on the top
- Write the slogan text in the middle using the chosen color
- Use a clean readable font

Save behavior:
- When I click “Save Design”, send the slogan, product, color, and timestamp to the backend
- The backend should save it into designs.json (append to array)
- Show a success message after saving

Backend:
- Express server running on port 3000
- Serve the frontend from /public
- POST /saveDesign endpoint to store the data
- If designs.json does not exist, create it automatically

Extra:
- Keep the UI clean and mobile friendly
- No login, no payment, no authentication
- No unnecessary complexity

Please generate the full working code for all files.
Return each file in a separate code block and label them with the file name.
Don’t explain anything — just give the code.
