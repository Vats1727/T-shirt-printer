{
  "name": "tshirt-printer",
  "private": true,
  "scripts": {
    "start": "npm --prefix server start",
    "dev": "start-server-and-test 'npm --prefix server start' http://localhost:5000 'npm --prefix client run dev'",
    "dev:client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "@types/node": "^25.0.10",
    "start-server-and-test": "^1.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
